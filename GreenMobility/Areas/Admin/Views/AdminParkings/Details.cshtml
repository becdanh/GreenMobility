@model GreenMobility.Models.Parking

@{
    ViewData["Title"] = "Thông tin bãi đỗ";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="page-header no-gutters">
    <div class="row align-items-md-center">
        <div class="col-md-7">
            <h2 class="header-title">Quản lý bãi đỗ</h2>
            <div class="header-sub-title">
                <nav class="breadcrumb breadcrumb-dash">
                    <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Trang chủ</a>
                    <a asp-area="Admin" asp-controller="AdminParkings" asp-action="Index" class="breadcrumb-item">Danh sách bãi đỗ</a>
                    <span class="breadcrumb-item active">Thông tin bãi đỗ</span>
                </nav>
            </div>
        </div>
        <div class="col-md-5">
            <div class="text-md-right d-flex justify-content-end">
                <div class="d-flex align-items-center">
                    <a class="btn btn-primary btn-tone mr-2" asp-area="Admin" asp-controller="AdminParkings" asp-action="Edit" asp-route-id="@Model.ParkingId">Sửa đổi</a>
                    <form id="deleteForm" method="post" asp-action="Delete" asp-route-id="@Model.ParkingId" class="mr-2">
                        <button type="submit" class="btn btn-danger btn-tone">Xóa</button>
                    </form>
                    <a asp-area="Admin" asp-controller="AdminParkings" asp-action="Index" class="btn btn-success btn-tone">Quay lại</a>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="tab-content m-t-15">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Thông tin bãi đỗ</h4>
        </div>
        <div class="card-body">
            <div class="media align-items-center">
                <div class="img-fluid m-h-10 m-r-15" style="height: 80px; width: 120px">
                    <img id="avatarPreview" src="~/images/parkings/@Model.Photo" alt="Avatar Preview" style="object-fit: contain; width: 100%; height: 100%;">
                </div>

                <div class="m-l-20 m-r-20">
                    <h5 class="m-b-5 font-size-18">@Model.ParkingName - @Model.Address</h5>
                </div>
            </div>
            <hr class="m-v-25">
            <div class="table-responsive">
                <table class="product-info-table">
                    <tbody>
                        <tr>
                            <td>Tên bãi đỗ:</td>
                            <td class="text-dark font-weight-semibold">@Model.ParkingName</td>
                        </tr>
                        <tr>
                            <td>Alias:</td>
                            <td>@Model.Alias</td>
                        </tr>
                        <tr>
                            <td>Địa chỉ:</td>
                            <td>@Model.Address</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<script>
    document.getElementById("deleteForm").addEventListener("submit", function (event) {
        event.preventDefault();

        if (confirm("Bạn có chắc chắn muốn xóa bãi đỗ này?")) {
            this.submit();
        }
    });
</script>